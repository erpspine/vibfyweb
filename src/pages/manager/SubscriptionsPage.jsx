import { Check, MoreHorizontal, Plus, WalletCards, Zap } from 'lucide-react'
import { plans } from '../../data/mockData'
import { Avatar, Badge, PageHeading, SectionHeader } from '../../components/ui'

export default function SubscriptionsPage() { return <><PageHeading badge={<Badge tone="purple"><WalletCards size={13} /> Billing control</Badge>} title="Subscriptions" description="Create plans and monitor recurring platform revenue."><button className="primary-button"><Plus size={18} />Create plan</button></PageHeading><div className="plan-grid">{plans.map(plan => <article className={`pricing-card ${plan.popular ? 'popular' : ''}`} key={plan.name}>{plan.popular && <Badge tone="purple">Most popular</Badge>}<span className={`plan-glyph ${plan.tone}`}><Zap /></span><h2>{plan.name}</h2><p>{plan.desc}</p><div className="price"><small>TSh</small><strong>{plan.price}</strong><span>/mo</span></div><ul>{plan.features.map(item => <li key={item}><Check size={15} />{item}</li>)}</ul><button className={plan.popular ? 'primary-button' : 'secondary-button'}>Manage plan</button></article>)}</div><section className="panel subscribers"><SectionHeader title="Recent subscriptions" subtitle="Latest plan activity" action="View all" /><div className="subscription-row"><Avatar /><strong>Rafiki Garden & Grill</strong><span>Growth plan</span><Badge tone="green">Active</Badge><span>TSh 129,000</span><MoreHorizontal /></div><div className="subscription-row"><Avatar /><strong>Kibo Rooftop</strong><span>Pro plan</span><Badge tone="green">Active</Badge><span>TSh 249,000</span><MoreHorizontal /></div></section></> }
